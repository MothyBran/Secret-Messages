<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Secret Messages - AES Hybrid Encryption System</title>
    <meta name="description" content="Militärgrad-sichere Verschlüsselung mit AES-256 Hybrid-Technologie">
    <style>
        /* === AUTO-LOGIN SYSTEM STYLES === */
        /* Loading Screen */
        .loading-screen {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 9999;
            color: white;
        }
        
        .loading-spinner {
            width: 50px;
            height: 50px;
            border: 5px solid rgba(255,255,255,0.3);
            border-top: 5px solid white;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-bottom: 20px;
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        /* Auth Screen */
        .auth-screen {
            min-height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        
        .auth-container {
            background: white;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            max-width: 500px;
            width: 100%;
            text-align: center;
        }
        
        .security-badge {
            display: inline-block;
            background: #e3f2fd;
            color: #1976d2;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 500;
            margin-bottom: 20px;
        }
        
        /* Logout Button */
        .logout-btn {
            position: fixed;
            top: 20px;
            right: 20px;
            background: #ff6b6b;
            color: white;
            border: none;
            padding: 8px 16px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
            z-index: 1000;
            transition: background 0.3s;
        }
        
        .logout-btn:hover {
            background: #e55656;
        }
        
        /* Demo Key Button */
        .demo-key-btn {
            background: transparent;
            color: #667eea;
            border: 2px solid #667eea;
            padding: 8px 16px;
            border-radius: 6px;
            font-size: 14px;
            cursor: pointer;
            margin-top: 15px;
            transition: all 0.3s;
            display: block;
            width: 100%;
        }
        
        .demo-key-btn:hover {
            background: #667eea;
            color: white;
        }
        
        /* Verstecke Sections standardmäßig */
        #licenseSection, #encryptionSection {
            display: none;
        }
        
        /* Animations */
        .fade-in {
            animation: fadeIn 0.5s ease-in;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        /* CSS Reset & Base Styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Courier New', monospace;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
            color: #00ff41;
            min-height: 100vh;
            overflow-x: hidden;
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header */
        .header {
            text-align: center;
            margin-bottom: 40px;
            position: relative;
        }

        .logo {
            font-size: 3rem;
            font-weight: bold;
            margin-bottom: 10px;
            text-shadow: 0 0 20px rgba(0, 255, 65, 0.6);
            background: linear-gradient(45deg, #00ff41, #00aa22);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .tagline {
            color: #888;
            font-size: 1.2rem;
            margin-bottom: 10px;
        }

        .version {
            color: #555;
            font-size: 0.9rem;
        }

        .admin-link {
            position: absolute;
            top: 0;
            right: 0;
            padding: 10px 20px;
            background: rgba(0, 255, 65, 0.2);
            border: 1px solid #00ff41;
            color: #00ff41;
            text-decoration: none;
            border-radius: 5px;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }

        .admin-link:hover {
            background: rgba(0, 255, 65, 0.3);
            transform: translateY(-2px);
        }

        /* Connection Status */
        .connection-status {
            background: rgba(255, 165, 0, 0.2);
            border: 1px solid #ffa500;
            color: #ffaa00;
            padding: 15px;
            border-radius: 10px;
            text-align: center;
            margin-bottom: 30px;
            animation: pulse 2s infinite;
        }

        /* Main Sections */
        .section {
            background: rgba(0, 255, 65, 0.05);
            border: 1px solid rgba(0, 255, 65, 0.3);
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            backdrop-filter: blur(10px);
        }

        .section-title {
            color: #00ff41;
            font-size: 1.5rem;
            margin-bottom: 20px;
            text-shadow: 0 0 10px rgba(0, 255, 65, 0.3);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        /* License Section */
        .license-section {
            text-align: center;
        }

        .license-description {
            color: #888;
            margin-bottom: 25px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .license-input {
            width: 100%;
            max-width: 400px;
            padding: 15px;
            margin: 20px auto;
            background: rgba(0, 0, 0, 0.7);
            border: 2px solid #00ff41;
            border-radius: 10px;
            color: #00ff41;
            font-family: 'Courier New', monospace;
            font-size: 1.1rem;
            text-align: center;
            letter-spacing: 2px;
            text-transform: uppercase;
            transition: all 0.3s ease;
            display: block;
        }

        .license-input:focus {
            outline: none;
            border-color: #00ff88;
            box-shadow: 0 0 20px rgba(0, 255, 65, 0.4);
            transform: scale(1.02);
        }

        .license-input::placeholder {
            color: #555;
            letter-spacing: normal;
            text-transform: none;
        }

        /* Buttons */
        .btn {
            padding: 15px 30px;
            border: none;
            border-radius: 8px;
            font-family: 'Courier New', monospace;
            font-weight: bold;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            margin: 10px;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(45deg, #00ff41, #00cc33);
            color: #000;
        }

        .btn-primary:hover {
            background: linear-gradient(45deg, #00cc33, #00aa22);
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(0, 255, 65, 0.4);
        }

        .btn-secondary {
            background: rgba(0, 255, 65, 0.2);
            color: #00ff41;
            border: 1px solid #00ff41;
        }

        .btn-secondary:hover {
            background: rgba(0, 255, 65, 0.3);
            transform: translateY(-2px);
        }

        .btn-danger {
            background: linear-gradient(45deg, #ff4444, #cc0000);
            color: #fff;
        }

        .btn-danger:hover {
            background: linear-gradient(45deg, #cc0000, #aa0000);
        }

        .btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none;
        }

        /* Encryption Section */
        .encryption-section {
            display: none;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-label {
            display: block;
            color: #00ff41;
            margin-bottom: 8px;
            font-weight: bold;
            font-size: 1rem;
        }

        .form-input, .form-textarea {
            width: 100%;
            padding: 15px;
            background: rgba(0, 0, 0, 0.7);
            border: 1px solid #00ff41;
            border-radius: 8px;
            color: #00ff41;
            font-family: 'Courier New', monospace;
            font-size: 1rem;
            transition: all 0.3s ease;
            resize: vertical;
        }

        .form-textarea {
            min-height: 120px;
            resize: vertical;
        }

        .form-input:focus, .form-textarea:focus {
            outline: none;
            border-color: #00ff88;
            box-shadow: 0 0 15px rgba(0, 255, 65, 0.3);
        }

        .code-input {
            max-width: 200px;
            text-align: center;
            font-size: 1.2rem;
            letter-spacing: 3px;
            text-transform: uppercase;
        }

        /* Output Areas */
        .output-group {
            position: relative;
            margin-top: 20px;
        }

        .output-textarea {
            width: 100%;
            min-height: 100px;
            padding: 15px;
            background: rgba(0, 255, 65, 0.1);
            border: 2px solid #00ff41;
            border-radius: 8px;
            color: #00ff41;
            font-family: 'Courier New', monospace;
            font-size: 0.9rem;
            resize: vertical;
            display: none;
        }

        .copy-btn {
            position: absolute;
            top: 10px;
            right: 10px;
            padding: 8px 12px;
            background: rgba(0, 255, 65, 0.2);
            border: 1px solid #00ff41;
            color: #00ff41;
            border-radius: 5px;
            font-size: 0.8rem;
            cursor: pointer;
            display: none;
            transition: all 0.3s ease;
        }

        .copy-btn:hover {
            background: rgba(0, 255, 65, 0.3);
        }

        /* Action Buttons */
        .action-buttons {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
            justify-content: center;
            margin-top: 25px;
        }

        /* Grid Layout */
        .crypto-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-top: 30px;
        }

        /* Features Section */
        .features {
            background: rgba(0, 255, 65, 0.02);
            border: 1px solid rgba(0, 255, 65, 0.2);
            border-radius: 15px;
            padding: 25px;
            margin: 30px 0;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .feature-item {
            padding: 20px;
            background: rgba(0, 255, 65, 0.05);
            border: 1px solid rgba(0, 255, 65, 0.3);
            border-radius: 10px;
            text-align: center;
        }

        .feature-icon {
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .feature-title {
            color: #00ff41;
            font-size: 1.1rem;
            margin-bottom: 8px;
            font-weight: bold;
        }

        .feature-description {
            color: #888;
            font-size: 0.9rem;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                padding: 15px;
            }

            .logo {
                font-size: 2rem;
            }

            .crypto-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }

            .admin-link {
                position: static;
                display: inline-block;
                margin-top: 20px;
            }

            .action-buttons {
                flex-direction: column;
                align-items: center;
            }

            .btn {
                width: 100%;
                max-width: 300px;
            }

            .features-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Animations */
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.02); }
            100% { transform: scale(1); }
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out;
        }

        @keyframes glow {
            0% { box-shadow: 0 0 5px rgba(0, 255, 65, 0.3); }
            50% { box-shadow: 0 0 20px rgba(0, 255, 65, 0.6); }
            100% { box-shadow: 0 0 5px rgba(0, 255, 65, 0.3); }
        }

        .glow {
            animation: glow 2s infinite;
        }

        /* Scrollbar Styling */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: rgba(0, 0, 0, 0.3);
        }

        ::-webkit-scrollbar-thumb {
            background: rgba(0, 255, 65, 0.5);
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: rgba(0, 255, 65, 0.7);
        }
    </style>
</head>
<body>
    <div id="loadingScreen" class="loading-screen">
        <div class="loading-spinner"></div>
        <h2>🔐 Secret Messages</h2>
        <p>Checking authentication status...</p>
    </div>
    
    <div class="container">
        <!-- Header -->
        <div class="header">
            <a href="/admin" class="admin-link">🔧 Admin Panel</a>
            <h1 class="logo">🔐 SECRET MESSAGES</h1>
            <p class="tagline">AES HYBRID ENCRYPTION SYSTEM</p>
            <p class="version">v13.0</p>
        </div>

        <!-- Connection Status -->
        <div id="connectionStatus" class="connection-status" style="display: none;">
            <div id="statusText">Verbindung zum Server wird geprüft...</div>
        </div>

        <!-- License Key Section -->
        <div id="licenseSection" class="section license-section">
            <h2 class="section-title">
                🔒 SECURE BACKEND-AUTHENTIFIZIERUNG
            </h2>
            
            <p class="license-description">
                Geben Sie Ihren persönlichen Lizenz-Key ein, um Zugang zu diesem 
                militärgrad-sicheren Verschlüsselungssystem zu erhalten. Das Backend 
                validiert Ihren Key und bindet ihn sicher an Ihr Gerät.
            </p>

            <input 
                type="text" 
                id="licenseKey" 
                class="license-input" 
                placeholder="XXXXX-XXXXX-XXXXX"
                maxlength="17"
                autocomplete="off"
            >

            <div class="action-buttons">
                <button id="activateBtn" class="btn btn-primary">
                    ZUGANG AKTIVIEREN
                </button>
                <button id="demoBtn" class="btn btn-secondary">
                    Demo Key anzeigen
                </button>
            </div>
        </div>
        
        <!-- License Key Section hinzufügen -->
        <div id="licenseSection" class="auth-screen">
            <div class="auth-container">
                <div class="security-badge">
                    🛡️ SECURE BACKEND AUTHENTICATION
                </div>
                <h1>🔐 Zugangsverifikation</h1>
                <p style="color: #666; margin-bottom: 30px;">
                    Geben Sie Ihren License Key ein, um auf die verschlüsselte Anwendung zuzugreifen
                </p>
                
                <div style="margin-bottom: 20px; text-align: left;">
                    <label style="display: block; margin-bottom: 5px; color: #333; font-weight: 500;">
                        License Key:
                    </label>
                    <input type="text" 
                           id="licenseKey" 
                           placeholder="SM001-ALPHA-BETA1"
                           maxlength="17"
                           style="width: 100%; padding: 12px 15px; border: 2px solid #e1e5e9; border-radius: 8px; font-size: 16px; font-family: monospace; letter-spacing: 1px; box-sizing: border-box;">
                </div>
                
                <button onclick="activateLicenseKey()" 
                        id="activateBtn"
                        style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; border: none; padding: 12px 30px; border-radius: 8px; font-size: 16px; font-weight: 500; cursor: pointer; width: 100%; margin-bottom: 15px;">
                    🔓 ZUGANG AKTIVIEREN
                </button>
                
                <button class="demo-key-btn" onclick="showDemoKey()">
                    🧪 Demo Key verwenden
                </button>
                
                <!-- Status Messages Container -->
                <div id="statusMessages"></div>
            </div>
        </div>
        
        <!-- Encryption Section -->
        <div id="encryptionSection" class="section encryption-section">
            <h2 class="section-title">
                🛡️ AES-256 VERSCHLÜSSELUNG
            </h2>

            <!-- Logout Button hinzufügen -->
            <button class="logout-btn" onclick="logout()" style="display: none;">🚪 Zugang LÖSCHEN</button>
    
            <!-- Shared Code Input -->
            <div class="form-group">
                <label class="form-label" for="codeInput">
                    🔑 Sicherheitscode (5 Zeichen):
                </label>
                <input 
                    type="text" 
                    id="codeInput" 
                    class="form-input code-input" 
                    placeholder="12345"
                    maxlength="5"
                    pattern="[0-9A-Za-z]{5}"
                    title="Genau 5 Zeichen (Buchstaben oder Zahlen)"
                >
            </div>

            <!-- Crypto Grid -->
            <div class="crypto-grid">
                <!-- Encryption Panel -->
                <div>
                    <h3 class="section-title">📤 VERSCHLÜSSELN</h3>
                    
                    <div class="form-group">
                        <label class="form-label" for="messageInput">
                            Nachricht eingeben:
                        </label>
                        <textarea 
                            id="messageInput" 
                            class="form-textarea" 
                            placeholder="Geben Sie hier Ihre geheime Nachricht ein..."
                        ></textarea>
                    </div>

                    <button id="encryptBtn" class="btn btn-primary" style="width: 100%;">
                        🔒 VERSCHLÜSSELN
                    </button>

                    <div class="output-group">
                        <textarea 
                            id="encryptedOutput" 
                            class="output-textarea" 
                            placeholder="Verschlüsselter Text erscheint hier..."
                            readonly
                        ></textarea>
                        <button id="copyEncryptedBtn" class="copy-btn">📋 Kopieren</button>
                    </div>
                </div>

                <!-- Decryption Panel -->
                <div>
                    <h3 class="section-title">📥 ENTSCHLÜSSELN</h3>
                    
                    <div class="form-group">
                        <label class="form-label" for="encryptedInput">
                            Verschlüsselten Text eingeben:
                        </label>
                        <textarea 
                            id="encryptedInput" 
                            class="form-textarea" 
                            placeholder="Fügen Sie hier den verschlüsselten Text ein..."
                        ></textarea>
                    </div>

                    <button id="decryptBtn" class="btn btn-primary" style="width: 100%;">
                        🔓 ENTSCHLÜSSELN
                    </button>

                    <div class="output-group">
                        <textarea 
                            id="decryptedOutput" 
                            class="output-textarea" 
                            placeholder="Entschlüsselter Text erscheint hier..."
                            readonly
                        ></textarea>
                        <button id="copyDecryptedBtn" class="copy-btn">📋 Kopieren</button>
                    </div>
                </div>
            </div>

            <!-- Additional Actions -->
            <div class="action-buttons">
                <button id="clearBtn" class="btn btn-danger">
                    🗑️ Alle Felder löschen
                </button>
            </div>
        </div>

        <!-- Features Section -->
        <div class="features">
            <h2 class="section-title">✨ FEATURES</h2>
            
            <div class="features-grid">
                <div class="feature-item">
                    <div class="feature-icon">🛡️</div>
                    <div class="feature-title">AES-256 Hybrid</div>
                    <div class="feature-description">
                        Militärgrad-sichere Verschlüsselung mit doppelter Sicherheitsschicht
                    </div>
                </div>
                
                <div class="feature-item">
                    <div class="feature-icon">⚡</div>
                    <div class="feature-title">Instant Processing</div>
                    <div class="feature-description">
                        Blitzschnelle Ver- und Entschlüsselung direkt im Browser
                    </div>
                </div>
                
                <div class="feature-item">
                    <div class="feature-icon">🌐</div>
                    <div class="feature-title">Universal</div>
                    <div class="feature-description">
                        Kompatibel mit allen Messengern und Plattformen weltweit
                    </div>
                </div>
                
                <div class="feature-item">
                    <div class="feature-icon">🔒</div>
                    <div class="feature-title">Zero Knowledge</div>
                    <div class="feature-description">
                        Ihre Nachrichten verlassen niemals Ihr Gerät unverschlüsselt
                    </div>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <div style="text-align: center; color: #555; margin-top: 40px; padding: 20px;">
            <p>&copy; 2024 Secret Messages. Alle Rechte vorbehalten.</p>
            <p style="font-size: 0.8rem; margin-top: 5px;">
                End-to-End Verschlüsselung für maximale Sicherheit
            </p>
        </div>
    </div>

    <!-- External JavaScript -->
    <script src="app.js"></script>
</body>
</html>
